spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root
    type: com.zaxxer.hikari.HikariDataSource
    hikari:
      maximum-pool-size: 32
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none
  sql:
    init:
      mode: always
server:
 address: 127.0.0.1
  port: 8088
---
spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/certificatesdb
  config:
    activate:
      on-profile: dev
---
spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/certificatesdb
  config:
    activate:
      on-profile: prod